### remove any local paths in scripts
### remove local_scripts/filter_signatures.R ? 
##### code is full of debug/comparisons to nathans data, and says that the final filtering was done on cluster using filter_lincs_exemplar_matched.R
Although NOTE: that script says "acc to hamonize_ids() function in local script ~/DrugSignatures/LINCS2020/filter_signatures.R)" so maybe keep that part
- hamonize_ids() is not in that script; from where then? 


### unsure where classN_signatures.txt comes from; theres local file medication_sample_sizes_UKB.R and medications_atc.txt
